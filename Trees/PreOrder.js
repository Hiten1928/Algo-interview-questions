var TreeNode = function(val){
    this.val = val;
    this.left = right = null;
}

var PreOrder = function(root) {
    let stack = [], arr = [];

    while(stack.length) {
        if(root) {

        }
    }
}